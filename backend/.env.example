# Gemini API Key (Required for real AI coaching insights)
GEMINI_API_KEY=your_gemini_api_key_here

# Firebase Admin SDK Credentials Path (Optional, falls back to local JSON DB if missing)
# FIREBASE_CREDENTIALS_PATH=path/to/firebase-credentials.json
